import * as t from "../types";

const initialState = {
  shop:{},
};

const newsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default newsReducer;
